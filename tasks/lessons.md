# Lessons

- When a user requests implementation of an approved plan, execute immediately and deliver verified code changes instead of additional analysis-only output.
- When a user reports a UI bug from a screenshot, verify both data-layer normalization/deduping and presentation-layer filtering logic instead of assuming it is only a styling issue.
